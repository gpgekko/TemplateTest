#{if id == "B" && test.id == "Parent"}
{
#{/if}
	"eid": "${test.id}",
	"children":
	[
	#{list test?.children, as:'item'}
		{
		#{set test:item/}
		#{include 'Application/templateB.json'/}
	#{/list}
	]
}